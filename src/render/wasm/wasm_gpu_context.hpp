#ifndef SKITY_SRC_RENDER_WASM_WASM_GPU_CONTEXT_HPP
#define SKITY_SRC_RENDER_WASM_WASM_GPU_CONTEXT_HPP

#include <skity/gpu/gpu_context.hpp>

namespace skity {}

#endif  // SKITY_SRC_RENDER_WASM_WASM_GPU_CONTEXT_HPP